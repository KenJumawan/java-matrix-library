# java-matrix-library
This project is a Java implementation of a matrix library that supports common operations such as addition, subtraction, multiplication, transpose, and matrix comparison. 
It also includes custom exception handling for invalid dimensions and basic test coverage for core operations. The project demonstrates object-oriented design, numerical logic, and error handling in Java.
